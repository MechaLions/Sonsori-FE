import { Button } from "@ui/components/ui/button";

const TranslatePage = () => {
    return (
    <main
        className="translate-background flex w-full flex-col justify-around"
        style={{ height: "calc(100vh - 88px)" }}
        >
        </main>
    );
}

export default TranslatePage;