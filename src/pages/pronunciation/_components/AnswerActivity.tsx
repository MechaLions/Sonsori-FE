import { ActivityComponentType } from "@stackflow/react";
import { AppScreen } from "@stackflow/plugin-basic-ui";

const AnswerActivity: ActivityComponentType = () => {
  return (
    <AppScreen>
      <main></main>
    </AppScreen>
  );
};

export default AnswerActivity;
